<ul>
  {{#each posts}}
    <li>
      <a href="{{#if logged_in}}/post/{{this.id}}{{else}}#{{/if}}" class="post-link">
        <h3>{{this.title}}</h3>
        <p>Author: {{this.User.username}} - Posted: {{this.date_created}}</p>
      </a>
    </li>
  {{/each}}
</ul>